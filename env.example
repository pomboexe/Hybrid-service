# ============================================
# VARIÁVEIS OBRIGATÓRIAS
# ============================================

# Database Configuration
# Credenciais do PostgreSQL (usadas pelo Docker Compose)
POSTGRES_USER=hybrid_user
POSTGRES_PASSWORD=hybrid_password
POSTGRES_DB=hybrid_service

# Database URL (para usar com Docker Compose - use as credenciais acima):
DATABASE_URL=postgresql://hybrid_user:hybrid_password@localhost:5433/hybrid_service

# Para usar com PostgreSQL manual (sem Docker):
# DATABASE_URL=postgresql://usuario:senha@localhost:5433/nome_do_banco

# Session Secret (obrigatório para autenticação)
# Gere uma string aleatória segura. Exemplo: openssl rand -base64 32
SESSION_SECRET=sua_chave_secreta_super_segura_aqui_mude_essa_string

# ============================================
# VARIÁVEIS OPCIONAIS
# ============================================

# Server Configuration
PORT=5000
NODE_ENV=development

# Authentication (opcional)
# Se definir como "true", a autenticação será obrigatória
REQUIRE_AUTH=false
